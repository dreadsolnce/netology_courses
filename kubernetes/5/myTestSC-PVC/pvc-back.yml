apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-vol-back
spec:
  storageClassName: sc-vol
#  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
