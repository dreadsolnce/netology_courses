apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend 
  labels:
    app: label-back
  namespace: app                     
spec:
  replicas: 2 # Колличество подов 
  selector:
    matchLabels:
      app: label-back
  template:
    metadata:
      labels:
        app: label-back
    spec:
      containers:
        - name: multitool
          image: praqma/network-multitool:alpine-extra
          ports:
            - containerPort: 80
              name: port


