apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: 
  name: policy-front
  namespace: app
spec:
  podSelector: 
    matchLabels:
      app: label-front
  policyTypes:
    - Ingress
  ingress:
    - from:
      - podSelector:
          matchLabels:
            app: label-test


