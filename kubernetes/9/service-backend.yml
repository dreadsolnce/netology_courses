apiVersion: v1
kind: Service
metadata:
  name: service-backend
  namespace: app
spec:
  selector:
    app: label-back 
  ports:
  - name: web-port-80
    protocol: TCP
    port: 80
    targetPort: port
