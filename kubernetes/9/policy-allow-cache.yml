apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: 
  name: policy-cache
  namespace: app
spec:
  podSelector: 
    matchLabels:
      app: label-cache
  policyTypes:
    - Ingress
  ingress:
    - from:
      - podSelector:
          matchLabels:
            app: label-back


