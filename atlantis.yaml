version: 3
projects:
  - name: my-infrastructure
    dir: diplom/main  # Папка, где лежат .tf файлы
    autoplan:
      when_modified: ["*.tf"] # Запускать plan автоматически при изменении tf-файлов
      enabled: true