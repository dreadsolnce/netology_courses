version: 3
projects:
  - name: diplom
    dir: diplom/main  # Папка, где лежат .tf файлы
    autoplan:
      when_modified: 
        - "*.tf" 
        - "!providers.tf" # Запускать plan автоматически при изменении tf-файлов
      enabled: true

