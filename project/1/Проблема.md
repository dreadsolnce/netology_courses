# Проблема создания подсетей через цикл

## Подробная информация о проблеме (ошибке), которую я задавал на вчерашнем вебинаре

В проекте есть два каталога: 
 - в одном из них я создаю подсети как два отдельных ресурса и одному из них я назначаю таблицу маршрутизации.
 - в другом я создаю подсети через цикл for each и через условие, если это приватная сеть, то я назначаю таблицу маршрутизации.

Единственное отличие этих двух проектов это в файле main.tf и vm.tf

### Файл main.tf

<img width="1024" height="792" alt="Снимок экрана от 2026-01-31 12-22-15" src="https://github.com/user-attachments/assets/ab02de34-ef80-4272-8574-7573603b3c04" />

### Файл vm.tf

<img width="1692" height="789" alt="Снимок экрана от 2026-01-31 12-22-52" src="https://github.com/user-attachments/assets/ee40d0b9-1b16-4ba7-9855-51f158b50433" />

___

#### Если я создаю подсети отдельно то код выполняется и все работает.

#### Если же я создаю подсети через цикл for each, то получаю следующую ошибку

<img width="1121" height="151" alt="Снимок экрана от 2026-01-31 12-25-10" src="https://github.com/user-attachments/assets/2ca70185-0cb2-4fff-823f-b2660317e196" />

При этом если комментирую строку подключения таблицы маршрутизации, то код выполняется, но естественно при подключении к back серверу из приватной сети я не имею доступа в интернет.

<img width="1110" height="410" alt="Снимок экрана от 2026-01-31 12-25-31" src="https://github.com/user-attachments/assets/047cbcbd-93c9-4fba-9f59-9c0b740f3171" />

### В чем может быть загвоздка?

#### p.s. сейчас ехал в машине и думал над проблемой и вот какому выводу я пришел, поэтому решил дополнить своими мыслями. Если мы создаем подсети отдельно они создаются как отдельные ресурсы (каждая подсеть это отдельный ресурс) и в таком случае терраформ после того как он разобрался с зависимостями создал вм и таблицу роутов он обращается именно к ресурсу подсети и добавляет в него переменную таблицы маршрутов. А во втором случае если мы создаем подсети циклом то по факту у нас создается один ресурс в котором как бы две подсети и обращение к подсети идет по ключу и поэтому терраформ обращаясь к ресурсу (в котором по факту две подсети) не может понять какую именно подсеть изменять. Поэтому он пытается создать сначала подсети, в одной из которых есть таблица, потом он идёт в таблицу в которой указано соэдай сначала вм, он идет в вм а там прописано создай сначала подсеть и таким образом происходит зацикливание. 
Можно ли вообще тогда в данном случае как то использовать цикл?🤔
